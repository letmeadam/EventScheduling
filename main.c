#include <stdio.h>
#include <stdlib.h>
#include "events.h"

// #define EVENT_FILE "my_events"

int main()
{
	printf("MAIN PROGRAM!\n");
	// FILE *filo;

	// if (NULL == (filo = fopen(EVENT_FILE, "r")))
	// 	return EXIT_FAILURE;

	// while (0 < fscanf(filo, "%d_%s_", ... ))
	// {
	// 	a
	// }

    return EXIT_SUCCESS;
}
